// PLACEHOLDER — not a real business/legal number. Needs founder + attorney sign-off
// before this app is used with real users. Every Match snapshots this value at the
// time it's created, so changing it here only affects future matches.
export const COPYRIGHT_SHARE_PERCENT = 10;
